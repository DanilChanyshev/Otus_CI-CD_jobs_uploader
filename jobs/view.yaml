---
- view:
    name: QA
    view-type: list
    description: "QA"
    filter-executors: true
    filter-queue: true
    job-name:
      - jobs_uploader
      - api-tests
      - ui-test
      - mobile-tests
      - running-tests
      - android-emulator
    columns:
      - status
      - job
      - build-button
      - weather

- view:
    name: Helpers
    view-type: list
    description: "Helpers"
    filter-executors: true
    filter-queue: true
    job-name:
      - jobs_uploader
      - android-emulator
    columns:
      - status
      - job
      - build-button
      - weather

- view:
    name: Test_runner
    view-type: list
    description: "Test_runner"
    filter-executors: true
    filter-queue: true
    job-name:
      - api-tests
      - ui-test
      - mobile-tests
      - running-tests
    columns:
      - status
      - job
      - build-button
      - weather